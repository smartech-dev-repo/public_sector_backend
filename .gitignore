node_modules
dist
.env
*.log
.DS_Store

# Real cloud service-account credentials — never committed.
gcp-credentials.json

/src/generated
/storage

# Real personal data (BVN, bank accounts, phone numbers, names) — reference
# material for design work only, never committed.
/docs/added
